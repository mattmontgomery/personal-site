import _ from 'lodash';

var projectBase = {
    name: '',

};

var projects = [
    _.extend(projectBase, {
        name: 'RSL Soapbox',
        role: 'Managing editor'
    })
];

export default projects;
